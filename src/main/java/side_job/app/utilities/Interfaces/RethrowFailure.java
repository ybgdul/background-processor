package side_job.app.utilities.Interfaces;

//this will be implemented to custom exception types so we can filter them, it holds no other purpose nor creates contracts
public interface RethrowFailure {
    
}
